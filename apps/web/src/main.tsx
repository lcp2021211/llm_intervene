import React from "react";
import ReactDOM from "react-dom/client";
import "c1-alibaba-puhui-ti";
import App from "./App.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
